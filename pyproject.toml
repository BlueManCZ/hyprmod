[project]
name = "hyprmod"
version = "0.4.0"
description = "A native GTK4/libadwaita settings app for Hyprland"
license = "GPL-3.0-or-later"
requires-python = ">=3.12"
dependencies = [
    # PyGObject on PyPI already requires pycairo, but distro packages of it list
    # pycairo as optional. Every DrawingArea silently draws nothing without it,
    # so declare it here for packagers to see (#78).
    "pycairo>=1.16",
    "pygobject>=3.56.2",
    "hyprland-config>=0.9.14",
    "hyprland-schema>=0.7.1",
    "hyprland-state>=0.4.6",
    "hyprland-monitors>=0.9.0",
    "hyprland-socket>=0.12.2",
]

[dependency-groups]
dev = [
    "pygobject-stubs>=2.17.0",
    "pyright>=1.1",
    "pytest>=9.0",
    "ruff>=0.15.11",
]

[project.scripts]
hyprmod = "hyprmod.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.hooks.custom]

[tool.pyright]
include = ["hyprmod", "tests"]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I"]

[tool.ruff.lint.isort]
known-third-party = ["gi", "hyprland_config", "hyprland_schema", "hyprland_state", "hyprland_monitors", "hyprland_socket"]
